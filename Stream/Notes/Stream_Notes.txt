******************************Java 8 Features******************************
1. Lambda Expression
2. Streams
3. Date & Time API

******************************Problem before Java 8******************************
1. Too much boilerplate code
2. Lots of for-Loops
3. Hard to read and maintain
4. Prallel processing

******************************Streams_Features******************************
Java 8 Feature
Sequence of elements
Use to process collections(List, Set, Array) in simpler way(No Loops, No If-Else)
Its not a data structure, does not store data
Do not modify original data, create new processed result
Built support for parallel execution
Source -> Intermediate -> Terminal

******************************Stream Creation******************************
1. Using collections
2. Using Arrays
3. Using Stream.of()
4. Infinite Streams:
    1. generate()
            Generates values independently
            Uses a Supplier
            Use limit() to Stop
            Eg. Print Hello 10 times
    2. iterate()
            Generates values based on previous value
            Uses UnaryOperator
            Needs starting value
            Use limit() to Stop
            Eg. Print 1 to 100

*****************************Intermediate Operations******************************
1. filter() -> Predicate
2. map() -> Function
3. sorted() -> Comparator (Natural Order)
4. distinct() -> .equals()/hashCode()
5. limit()
6. skip()
7. peek()
8. flatMap() -> Function

*****************************Terminal Operations******************************

1. collect() -> Collector, Mutable
2. toList() -> (Java 16+), Unmodifiable List, Immutable
3. forEach() -> Consumer

.get() -> (If a value is present, returns the value, otherwise throws NoSuchElementException)
        4. reduce() -> Binary Operator, Reduce elements into single value
        5. findFirst() -> Short-Circuit Operations
        6. findAny() -> Short-Circuit Operations

7. count() -> 
8. anyMatch() -> Predicate (Boolean), Short-Circuit Operations
9. allMatch() -> Predicate (Boolean), Short-Circuit Operations
10. noneMatch() -> Predicate (Boolean), Short-Circuit Operations
11. min() -> Comparator.naturalOrder()
12. max() -> Comparator.naturalOrder()

******************************Parallel & Sequential Stream*********************************************
-> Parallel Stream: parallelStream() (Multiple Threads)
Allowing multiple threads to process the parts of stream concurrently
Workload is distibuted across multiple threads
Use When: CPU-Intensive operations, Large data sets, Independent operations
Order not gauranted
Eg. Find Factorial of Number

-> Stream: stream() (Single Thread)
Create sequeantial stream(default)
Process elements one by one
Order is preseved
Eg. Cumulative Sum

-> Sequential Stream: Sequential() (Single Thread)
Forces Stream to Run Sequentially
Eg. Parllel Stream (Find Factorial of Nums) -> Parallel To Sequential -> Find Cumulative Sum of all Factorila Values 

***************************Stateful & Stateless Operations***********************************************
-> Stateless:

filter()
map()
forEach()
anyMatch()
noneMatch()
allMatch()
findFirst()
findAny()
peek()
flatMap()

-> Stateful:

If an operation must remeber:
1. Elements seen so far, or
2. An accumulated result

distinct()
sorted()
limit()
skip()
reduce()
count()
collect()
toList()

*******************************map() VS mapToInt()****************************************************

-> Stream<T> (Object)
Intermediatory Operations: map() 
Terminal Operations: .toList(), .collect()
( Stream<String> -> Stream<Integer> -> List<Integer> )

-> IntStream / LongStream / DoubleStream
Intermediatory Operations: mapToInt(), mapToLong(), mapToDouble
Terminal Operations: .toArray(), .sum(), .average()
( Stream<String> -> InStream -> int[] )

map() -> conatains boxed objects
mapToInt() -> contains primitives

Java stream never do boxing/unboxing silently.
Crossing between object and primitive streams must be explicit.

Boxing:    Raw -> Object, eg. int -> Integer
Unboxing:  Object -> Raw, eg. Integer -> int

split() -> Returns String[]

****************************************Note*****************************************************************

-> Stream cannot be resued after terminal operation has been called
IllegalStateException: stream has already been operated upon or closed

*************************************forEachOrdered()*********************************************************
parallelStream():
toList(), forEachOrdered() -> Maintains Order
forEach() -> Doest Not Maintain Order

*******************************Collectors Utility Class**********************************************

Collectors.toList()
          .toSet()
          .toCollection(LinkedHashSet::new)
          .joining(delimiter:" ")
          .toMap()
          .summarizingInt()
          .summarizingDouble()
          .summingInt()
          .summingDouble()
          .averagingInt()
          .counting()
          .maxBy()
          .minBy()
          .groupingBy()
          .mapping()
          .partitioningBy()
          .collectingAndThen()

Map<String, String> map = new HashMap<>(); -> Mutable
Map<String, String> map = Map.of(); -> Immutable, Map.of() does not guarantee order

summarizingInt(x -> x) -> Collector, Converts elements to int
Instead of looping 5 times for 5 operations, it loop once.
Eg.
for(int num : nums){
        count++;
        sum =+ num;
        min = Math.min(min, num);
        max = Math.max(max, num)
}

mapToInt() -> Stream<Integer> to IntStream(Primitive Stream)

OptionalDounble -> Stream may be empty
max(), min(), average() -> In case of empty stream we cannot return 0, 
                           As 0 might be actual valid value in real data,
                           So we return optional.

            summing     vs   summarizing
Returns -> (Only Sum)        (Returns count, sum, max, min, avg)

* groupingBy() *        |     * partitioningBy() *
Any No of Groups        |          Always 2
Classifier Funstion     |    Predicate (true/false) (Boolean)
                        |   Returns ( Map<Boolean, List<T>> )


* filter() *            |      * partitioningBy() *
Removes elements        |    Divids into two groups
Returns Stream          |    Returns Map<Boolean, List<T>>

.collectingAndThen() -> Collects the data(For Eg. Grouped Data) then perform operations


